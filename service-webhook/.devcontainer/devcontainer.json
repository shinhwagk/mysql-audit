{
    "name": "Servcie Gitlab Webhook",
    "dockerFile": "Dockerfile",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "postCreateCommand": "npm install",
    "appPort": 3000
    // "extensions": [
    //     "dbaeumer.vscode-eslint"
    // ]
}